#include <stdio.h>
/**
*main- this is where the program starts from.
*Return:0 no error, non zero if there is any error
*/
int main(void)
{
	int i;
	for (i = 0; i <= 9; i++)
		putchar(i);
		putchar('\n');
	return (0);
}
