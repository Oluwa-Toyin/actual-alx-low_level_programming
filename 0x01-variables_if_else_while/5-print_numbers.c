#include <stdio.h>

/**
 *main- This is the program's entry point.
 *Return: If there is no error, the return is zero
 **/

int main(void)

{
	int i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}


