#include <stdio.h>

/**
 * main- This is the entry point
 * Return: The program returns zero is successful
 **/

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar ('\n');
	return (0);
}
