#include "main.h"
#include <stdio.h>

/* Main starts the program
 * 0 is returned if no error
 */
 int main (void)
{
	#include <stdio.h>

int main(void)
{
    int a[] = { 97, 98, 99 };
    int *p = a;

    p += 2;

    printf("a[2] = %d\n", *p); // added line of code

    return 0;
}

